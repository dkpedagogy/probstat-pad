# 검증 기록

검증 시각: 2026-09-23 (Asia/Seoul)

## 정적 검사

- `node --check out/worker/src/index.js`: 통과.
- `out/site/index.html`의 마지막 인라인 JavaScript를 임시 파일로 추출해 `node --check`: 통과.
- `out/site/teacher.html`의 마지막 인라인 JavaScript를 임시 파일로 추출해 `node --check`: 통과.
- `out/scripts/make_hash.html`의 인라인 JavaScript를 임시 파일로 추출해 `node --check`: 통과.

## 순수 함수 및 데이터 흐름 검사

- index 앱의 `computeStuck`와 `formatSeconds`를 Node VM에서 호출: 통과.
- 첫 획 전 30초 대기, 획 사이 20초 대기, 30초 내 지우기·실행취소 4회 규칙을 샘플 이벤트로 검사: 통과.
- `items.json`의 4문항을 읽어 item_id, 이미지, 해설, 답, 유형 스키마를 검사: 통과.
- `?debug=1` 마우스 경로는 코드의 허용 조건과 `stroke_start`·`stroke_end` 기록 호출을 정적으로 확인: 통과.
- 샘플 4문항 각각에 대해 열기·획·제출·평가·해설·요청·다음·닫기 상태를 생성하는 순수 상태 전이 검사: 통과.
- Worker KV 모의 객체로 학생 POST 2회, item 누적 보존, 교사 목록·상세 GET, OPTIONS 응답 검사: 통과.

## 미확인

- 이 환경에는 데스크톱 브라우저와 jsdom이 없어 실제 DOM Pointer Events, IndexedDB, File API, iPad Safari의 Apple Pencil 입력은 미확인.
- Worker KV 실배포, CORS 실제 응답, 네트워크 재시도는 미확인.
- 실제 문항 이미지가 아닌 placeholder PNG를 사용함.
